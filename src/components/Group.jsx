import React from 'react'

export default function Group({showList, setShowList, data}) {
  console.log(data)
  return
  // (
    // <div>
    //   {showList ? (
    //     data.map((movie) => (
    //       <div>
    //         <ul>
    //           <li>{movie}</li>
    //         </ul>
    //       </div>
    //     ))) : null}

    // </div>
  // )
}
